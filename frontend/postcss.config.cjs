// PostCSS is not used for Tailwind CSS v4 when the @tailwindcss/vite plugin is active.
// This file is kept as a placeholder. Add PostCSS plugins here only if you need
// processing that the Vite plugin does not cover (e.g. custom transforms).
module.exports = {
  plugins: {},
}
